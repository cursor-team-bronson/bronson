# Environment variables are defined per app — see:
#   apps/orchestrator/.env.example  — CLōD + PORT (required to run jobs)
#   apps/web/.env.example           — Supabase, optional ORCHESTRATOR_URL (copy to apps/web/.env.local)
